---
const NAV_ITEMS = [] as const

const currentPath = Astro.url.pathname
const shouldReloadNavItem = (href: string) => false
---

<header
  data-header
  data-menu-open="false"
  class="fixed inset-x-0 top-0 z-50 border-b border-transparent bg-transparent backdrop-blur-[0px] transition-[background-color,backdrop-filter,border-color] duration-500 ease-out data-[scrolled=true]:border-theme-purple-accent/20 data-[scrolled=true]:bg-theme-purple/60 data-[scrolled=true]:backdrop-blur-md"
>
  <div
    class="relative z-50 mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-3 sm:px-6 sm:py-4"
  >
    <a
      href="/"
      class="group flex items-center gap-3 outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-theme-gold"
      aria-label="NINJA RIFAS - Inicio"
    >
      <img
        src="/Logoo.png"
        alt=""
        class="h-9 w-auto transition duration-500 group-hover:rotate-[-2deg] sm:h-10"
        width="40"
        height="40"
        decoding="async"
      />
      <span
        class="hidden font-cinzel text-[0.6rem] font-bold tracking-[0.32em] text-white transition-all duration-300 ease-out group-hover:text-theme-gold group-hover:scale-[1.04] group-hover:drop-shadow-[0_0_12px_rgba(199,168,107,0.7)] group-focus-visible:text-theme-gold selection:bg-theme-gold selection:text-[#0a1024] sm:inline-block sm:text-xs"
      >
        NINJA RIFAS
      </span>
    </a>

    <nav class="hidden md:block" aria-label="Navegación principal">
      <ul class="flex items-center gap-1 sm:gap-2">
        {
          NAV_ITEMS.map((item) =>
              item.disabled ? (
              <li>
                <span
                  role="link"
                  aria-disabled="true"
                  class="relative inline-flex select-none items-center px-3 py-2 font-cinzel text-[0.65rem] font-bold tracking-[0.25em] text-white/40 sm:px-4 sm:text-xs"
                  title="Próximamente"
                >
                  {item.label}
                  {item.note && (
                    <span class="pointer-events-none absolute left-1/2 top-full hidden -translate-x-1/2 -translate-y-2 whitespace-nowrap rounded-full border border-theme-gold/40 bg-theme-gold/5 px-2 py-0.5 font-cinzel text-[0.5rem] font-semibold tracking-[0.22em] text-theme-gold/85 sm:inline-block sm:text-[0.55rem]">
                      {item.note}
                    </span>
                  )}
                </span>
              </li>
            ) : (
              <li>
                <a
                  href={item.href}
                  data-astro-reload={shouldReloadNavItem(item.href) ? true : undefined}
                  class="group relative inline-flex items-center px-3 py-2 font-cinzel text-[0.65rem] font-bold tracking-[0.25em] text-white/85 transition-all duration-300 ease-out outline-none hover:text-theme-gold hover:scale-[1.04] hover:drop-shadow-[0_0_12px_rgba(199,168,107,0.7)] focus-visible:text-theme-gold sm:px-4 sm:text-xs"
                >
                  {item.label}
                </a>
              </li>
            )
          )
        }
      </ul>
    </nav>

    <button
      type="button"
      data-mobile-menu-button
      class="group relative inline-flex h-11 w-11 items-center justify-center rounded-full border border-white/15 bg-black/20 text-theme-cream shadow-[0_0_30px_rgba(255,255,255,0.06)] outline-none backdrop-blur-md transition duration-300 hover:border-theme-gold/50 hover:bg-theme-gold/10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-theme-gold md:hidden"
      aria-controls="mobile-menu"
      aria-expanded="false"
      aria-label="Abrir menú"
    >
      <span class="sr-only">Abrir menú</span>
      <span
        aria-hidden="true"
        class="absolute h-px w-5 -translate-y-1.5 bg-current transition duration-300 group-data-[open=true]:translate-y-0 group-data-[open=true]:rotate-45"
      ></span>
      <span
        aria-hidden="true"
        class="absolute h-px w-5 bg-current transition duration-300 group-data-[open=true]:opacity-0"
      ></span>
      <span
        aria-hidden="true"
        class="absolute h-px w-5 translate-y-1.5 bg-current transition duration-300 group-data-[open=true]:translate-y-0 group-data-[open=true]:-rotate-45"
      ></span>
    </button>
  </div>

  <div
    id="mobile-menu"
    data-mobile-menu
    aria-hidden="true"
    inert
    class="fixed inset-0 z-40 flex min-h-dvh translate-y-[-8px] items-center justify-center overflow-y-auto bg-theme-purple/95 px-6 py-28 opacity-0 backdrop-blur-2xl transition duration-300 ease-out pointer-events-none data-[open=true]:translate-y-0 data-[open=true]:opacity-100 data-[open=true]:pointer-events-auto md:hidden"
  >
    <div
      aria-hidden="true"
      class="pointer-events-none absolute inset-x-0 top-0 h-64 bg-[radial-gradient(circle_at_50%_0%,rgba(199,168,107,0.22),transparent_62%)]"
    ></div>
    <nav aria-label="Menú principal móvil" class="relative w-full max-w-sm">
      <ul class="flex flex-col items-center gap-6">
        {
          NAV_ITEMS.map((item) =>
            item.disabled ? (
              <li class="w-full">
                <span
                  role="link"
                  aria-disabled="true"
                  class="flex flex-col items-center gap-2 rounded-2xl border border-white/10 bg-white/[0.03] px-5 py-5 text-center font-cinzel text-lg font-bold tracking-[0.28em] text-white/35"
                  title="Próximamente"
                >
                  {item.label}
                  {item.note && (
                    <span class="rounded-full border border-theme-gold/35 bg-theme-gold/10 px-3 py-1 text-[0.55rem] font-semibold tracking-[0.24em] text-theme-gold/80">
                      {item.note}
                    </span>
                  )}
                </span>
              </li>
            ) : (
              <li class="w-full">
                <a
                  href={item.href}
                  data-astro-reload={shouldReloadNavItem(item.href) ? true : undefined}
                  data-mobile-menu-link
                  class="group relative flex items-center justify-center rounded-2xl border border-white/15 bg-black/20 px-5 py-5 text-center font-cinzel text-lg font-bold tracking-[0.28em] text-white/85 outline-none transition-all duration-300 ease-out hover:border-theme-gold/40 hover:bg-theme-gold/10 hover:text-theme-gold hover:scale-[1.02] hover:drop-shadow-[0_0_15px_rgba(199,168,107,0.5)] focus-visible:text-theme-gold"
                >
                  {item.label}
                </a>
              </li>
            )
          )
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  import { $, $$ } from '@/lib/dom-selector'

  const SCROLL_THRESHOLD = 24

  let scrollAbortController: AbortController | null = null
  let menuAbortController: AbortController | null = null

  function setupHeaderScroll() {
    const header = $<HTMLElement>('[data-header]')
    if (!header) return

    scrollAbortController?.abort()
    scrollAbortController = new AbortController()

    const update = () => {
      const scrolled = window.scrollY > SCROLL_THRESHOLD
      if (scrolled) {
        header.setAttribute('data-scrolled', 'true')
      } else {
        header.removeAttribute('data-scrolled')
      }
    }

    update()

    window.addEventListener('scroll', update, {
      passive: true,
      signal: scrollAbortController.signal,
    })
  }

  function setupMobileMenu() {
    const header = $<HTMLElement>('[data-header]')
    const menu = $<HTMLElement>('[data-mobile-menu]')
    const button = $<HTMLButtonElement>('[data-mobile-menu-button]')
    const links = $$<HTMLAnchorElement>('[data-mobile-menu-link]')

    if (!header || !menu || !button) return

    menuAbortController?.abort()
    menuAbortController = new AbortController()
    const { signal } = menuAbortController

    const setMenuOpen = (isOpen: boolean) => {
      header.dataset.menuOpen = String(isOpen)
      menu.dataset.open = String(isOpen)
      button.dataset.open = String(isOpen)
      menu.setAttribute('aria-hidden', String(!isOpen))
      menu.inert = !isOpen
      button.setAttribute('aria-expanded', String(isOpen))
      button.setAttribute('aria-label', isOpen ? 'Cerrar menú' : 'Abrir menú')
      const label = $<HTMLElement>('.sr-only', button)
      if (label) label.textContent = isOpen ? 'Cerrar menú' : 'Abrir menú'
      document.documentElement.classList.toggle('overflow-hidden', isOpen)
    }

    setMenuOpen(false)

    button.addEventListener(
      'click',
      () => {
        setMenuOpen(button.getAttribute('aria-expanded') !== 'true')
      },
      { signal }
    )

    links.forEach((link) => {
      link.addEventListener('click', () => setMenuOpen(false), {
        signal,
      })
    })

    window.addEventListener(
      'keydown',
      (event) => {
        if (event.key === 'Escape') setMenuOpen(false)
      },
      { signal }
    )
  }

  document.addEventListener('astro:page-load', () => {
    setupHeaderScroll()
    setupMobileMenu()
  })
</script>
